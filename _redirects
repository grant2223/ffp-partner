# FFP Partner — partner.findfitpeople.com
# The dashboard guard (ffp-provider-auth.js) sends unauthenticated visitors to /login.
# Serve the partner login there, and fall everything else back to the dashboard shell.
# ORDER MATTERS: /login must come BEFORE the catch-all.

/login    /login.html    200
/*        /index.html    200
